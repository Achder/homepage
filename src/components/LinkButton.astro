---
type Props = {
    href: string
    class?: string
}

const { href, class: className, ...rest } = Astro.props
---

<a
    href={href}
    class:list={[
        className,
        'bg-dark rounded-xs text-brand hover:text-white px-8 py-6 font-extrabold tracking-wider flex gap-4 whitespace-nowrap',
    ]}
    {...rest}
>
    <slot />
</a>

<style>
    @container (max-width: 500px) {
        a {
            width: 100%;
            text-align: center;
        }
    }
</style>
