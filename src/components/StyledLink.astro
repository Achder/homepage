---
type Props = {
    href: string
    class?: string
}

const { href, class: className, ...rest } = Astro.props
---

<a
    href={href}
    class:list={[
        className,
        'transition-all hover:text-fuchsia-500 underline underline-offset-2 whitespace-nowrap',
    ]}
    {...rest}
>
    <p class="font-"></p>
    <slot />
</a>
